# simple-chatbot
A simple chatbot is an interactive application that allows users to type messages and receive automated replies. It is built with a basic user interface (chat window + input box) and a response engine. The chatbot can be rule‑based (predefined responses to keywords) or AI‑powered (using Google AI Studio or similar.
